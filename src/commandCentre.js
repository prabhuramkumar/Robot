import {place, rotate, move, report} from './actionCentre.js';


export default function receiveCommand(command){
    //Redirect command to actions
}

function distributeCommand(command){
    //Redirect command to actions
}

